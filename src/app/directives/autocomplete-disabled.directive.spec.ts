import { AutocompleteDisabledDirective } from './autocomplete-disabled.directive';

describe('AutocompleteDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
